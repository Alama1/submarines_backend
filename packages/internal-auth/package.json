{
  "name": "@ff14/internal-auth",
  "version": "1.0.0",
  "private": true,
  "description": "Shared internal token authentication for service-to-service and RabbitMQ traffic",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "build:watch": "tsc -p tsconfig.json --watch"
  },
  "peerDependencies": {
    "@nestjs/common": ">=10.0.0"
  },
  "devDependencies": {
    "@nestjs/common": "^11.0.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.0.0"
  }
}
