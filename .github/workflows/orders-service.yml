name: orders-service

on:
  push:
    branches: [main]
    paths:
      - 'apps/orders-service/**'
      - 'packages/shared-types/**'
      - 'package.json'
      - 'pnpm-workspace.yaml'
      - 'tsconfig.base.json'
  pull_request:
    branches: [main]
    paths:
      - 'apps/orders-service/**'
      - 'packages/shared-types/**'

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v4
        with:
          version: latest

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build shared types
        run: pnpm --filter @ff14/types build

      - name: Build orders-service
        run: pnpm --filter @ff14/orders-service build

      - name: Test orders-service
        run: pnpm --filter @ff14/orders-service test --passWithNoTests
